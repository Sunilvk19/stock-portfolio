spring:
  application:
    name: stock portfolio

  datasource:
    url: jdbc:mysql://localhost:3306/StockPortfolioManagement?createDatabaseIfNotExist=true
    username: root
    password: coder

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true